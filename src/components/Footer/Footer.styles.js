import styled from "styled-components";

export const Wrapper = styled.div`
  display: flex;
  justify-content: center;
  align-items: bottom;
  color: var(--highLightColor, gray);

  .moralisImage {
      margin: 0 5px 0 10px;
  }
`;

